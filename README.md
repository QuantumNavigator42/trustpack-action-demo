# TrustPack Action Demo

This repository demonstrates running TrustPack Action from the public v0.1.0 release.

The workflow writes the TrustPack report pack to `trustpack/` and uploads it as a GitHub Actions artifact named `trustpack`.

TrustPack helps collect repository-level security evidence. It is not legal, audit, or compliance certification.
